describe('test session service', function () {

    it('test session.store', function (done) {
        backend.store({
            name: "foo",
        })
    })
    describe('test session', function () {
        it('test session.login', function (done) {
            let body = {
            }
        })
    })



})